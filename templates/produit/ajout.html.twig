{% extends 'base.html.twig' %}
    {% block body %}
        <h1>Ajout Produit</h1>
        {{form_start(form)}}
        {{form_widget(form)}}
        <button type="submit">Ajouter</button>
        {{form_end(form)}}
    {% endblock  %}